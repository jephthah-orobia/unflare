name: Run-All-Test

on:
  - pull_request

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm install
      - run: npm run test:once
      - run: cd test*/test*/ & npm install
      - run: cd test*/test*/ & npm run test:once
